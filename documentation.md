# Reward related values
1. When speed > 0.1 m/s, the getWaitingTime value is reset to zero.  
2. The getAccumulatedWaitingTime has a time horizon, the default is 100s.  
3. Time loss is robust as there are no sudden changes during the simulation.
